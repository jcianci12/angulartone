import { IsBlackNotePipe } from './is-black-note.pipe';

describe('IsBlackNotePipe', () => {
  it('create an instance', () => {
    const pipe = new IsBlackNotePipe();
    expect(pipe).toBeTruthy();
  });
});
