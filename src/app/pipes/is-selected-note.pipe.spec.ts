import { IsSelectedNotePipe } from './is-selected-note.pipe';

describe('IsSelectedNotePipe', () => {
  it('create an instance', () => {
    const pipe = new IsSelectedNotePipe();
    expect(pipe).toBeTruthy();
  });
});
